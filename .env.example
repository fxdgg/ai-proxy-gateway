# 网关地址：PAC 里 PROXY 指向的 host:port。员工/桌面/OS 通过它连到 mihomo 网关。
# ⚠ 只填 host:port，不要带 http:// 前缀、不要带结尾斜杠（PAC 的 PROXY 指令只认 host:port）。
#   正确：GATEWAY_ADDR=10.0.0.8:10800   或   gateway.corp.example.com:10800
#   错误：GATEWAY_ADDR=http://10.0.0.8:10800
GATEWAY_ADDR=gateway.corp.example.com:10800
